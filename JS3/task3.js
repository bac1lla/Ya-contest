const sendForm = document.querySelector('.send__form'),
      name = document.querySelector('#user-name'),
      phone = document.querySelector('#user-phone'),
      email = document.querySelector('#user-email'),
      confirmEmail = document.querySelector('#user-name');

function confirmEmail(email, confirmEmail) {
    return
}

sendForm.addEventListener('click', validateForm(e));

function validateForm(form) {

}